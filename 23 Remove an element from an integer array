int remove_element ( int* arr, const int len,const int k)
{
	if (!arr || len <= 0) return 0;
	int i = 0;
	int new_len = 0;
	for (i=0; i < len; i++)
	{
		if (arr[i] != k)
		{
			arr[new_len] = arr[i];
			new_len++;
		}
	}
	return new_len;
}
